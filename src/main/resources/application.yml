server:
  port: 8085

spring:
  profiles:
    active: dev
  application:
    name: demo
  datasource:
    url: jdbc:mysql://localhost:3306/demo?allowMultiQueries=true&autoReconnect=true&useSSL=false
    username: root
    password: password
    driver-class-name: com.mysql.jdbc.Driver

  redis:
    database: 0
    host: localhost
    port: 6379
    password:
    timeout: 2000ms


jwt:
  tokenHeader: Authorization #JWT�洢������ͷ
  expiration: 1800 #JWT�ĳ�����ʱ��30����(60*30)
  tokenHead: Bearer #JWT�������õ���ͷ
